{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default permission set for the main window. Lite only needs fs (config push), dialog (file pickers), and store (saved server credentials).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "store:default"
  ]
}
